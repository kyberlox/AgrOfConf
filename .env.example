DOMAIN=localhost
HOST=http://localhost
user=user
pswd=password
POSTGRES_DB=pdb
DB_HOST=postgres
POSTGRES_PORT=5432

key_api = key_api
model_type = model_name
vseGPTurl = url
KIBANA_TOKEN=tkn

# DEV-сессия для локального тестирования (максимальные права).
# DEV_SESSION_ENABLED=false отключает её и возвращает штатную авторизацию.
DEV_SESSION_ENABLED=true
DEV_USER_ID=4133