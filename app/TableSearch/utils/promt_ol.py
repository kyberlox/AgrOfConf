from typing import Dict, Any
def get_promt(params: Dict[str, Any]) -> str:
    super_promt = f"""
    Мне нужно собрать json по шаблону:
    {params}

    из шаблона нужно взять названия параметров, найти совпадение в исходных данных из документа, который я прислал,
    нужно выбрать одно значение из доступных (там где предлагается список), это значение должно соответствовать исходному или самое близкое к нему (если среди предложенных его нет),
    если параметра из шаблона нет в исходных данных - пропусти и переходи к следующему.

    результирующий json должен состоять из пар ключ - значение, где ключи - и значения взяты из шаблона в соответсвии с исходным файлом.
    в ответ пришли только json!
    """
    return super_promt

VALIDATION_PROMPT = """
У тебя есть три входных данных:

1. **RAW_MD** — Markdown-таблица, извлечённая из документа vision-моделью.
   Формат: таблица с колонками | Параметр | Значение | Размерность |
   Подпараметры могут быть записаны как вложенные строки с дефисом.

2. **TEMPLATE_JSON** — эталонный JSON-шаблон, где:
   - Простой параметр: "параметр": [["допустимые_значения..."], "размерность"]
   - Параметр с подпараметрами: "параметр": {"подпараметр": [["допустимые_значения"], "размерность"], ...}
   - "*" в массиве значений — подходит любое значение

3. **RULES** — правила валидации и конвертации.

Твоя задача:

### Шаг 1: ПАРСИНГ MARKDOWN → СТРУКТУРА
Прочитай Markdown-таблицу и преобразуй её во внутреннюю структуру:
- Строки таблицы — это параметры
- Строки с дефисом ("- Подпараметр") — это подпараметры родительского параметра

### Шаг 2: СОПОСТАВЛЕНИЕ С TEMPLATE_JSON
Для каждого ключа из TEMPLATE_JSON найди соответствующий параметр в RAW_MD.
Сопоставляй по смыслу, а не по точному совпадению.

### Шаг 3: ВЫБОР И КОНВЕРТАЦИЯ ЗНАЧЕНИЯ
1. Возьми значение из RAW_MD
2. Найди в TEMPLATE_JSON массив допустимых значений
3. Если совпадает — используй его
4. Если не совпадает — выбери НАИБОЛЕЕ БЛИЗКОЕ
5. Если "*" — используй как есть
6. СКОНВЕРТИРУЙ значение к формату из TEMPLATE_JSON:
   - "12Х18Н9ТЛ" → "нержавеющая сталь (12Х18Н9ТЛ)"
   - "1.6 МПа" → "16 кгс/см²" (если в TEMPLATE размерность кгс/см²)

### Шаг 4: ФОРМИРОВАНИЕ РЕЗУЛЬТАТА
Верни JSON в формате: "параметр": "значение"

- Для простых параметров: "Номинальный диаметр": "100"
- Для параметров с подпараметрами: "Давление": {"рабочее": "1.6", "расчетное": "2.5"}
- Ключи ТОЛЬКО из TEMPLATE_JSON
- Значения ТОЛЬКО из списка допустимых (или ближайшие)
- Размерность НЕ включай
- Если параметра нет в RAW_MD — НЕ включай

В ответе пришли ТОЛЬКО JSON, без пояснений.
"""

UNIFIED_PROMPT = """
Из документа, который я прислал, извлеки все параметры и их значения.
Верни результат строго в формате Markdown-таблицы.

ПРАВИЛА ФОРМИРОВАНИЯ MARKDOWN:

1. Используй таблицу с колонками: | Параметр | Значение | Размерность |
   - Если размерности нет — оставь ячейку пустой
   
2. ПОДПАРАМЕТРЫ — если у параметра есть подпараметры, используй вложенный список
   под строкой параметра с отступом:
   
   | Давление (избыточное) | | |
   | - Рабочее | 1.6 | МПа |
   | - Настройка клапана | 1.8 | МПа |
   | - Расчетное | 2.5 | МПа |

3. ЧТО НЕ ЯВЛЯЕТСЯ ПОДПАРАМЕТРОМ:
   - Текст в скобках в названии параметра — это часть названия, НЕ подпараметр.
     ✅ | Давление (избыточное) | 1.6 | МПа |
   
   - Простое текстовое значение — это значение, а не подпараметр.
     ✅ | Тип уплотнения затвора | металл-металл | |
   
   - Список текстовых пунктов — объедини в одну строку через запятую.
     ✅ | Документация | Чертеж со спецификацией, ТУ, паспорт/РЭ | |

4. ВЫБОР ИЗ СПИСКА (ЧЕКБОКСЫ):
   - Найди, какое значение из списка отмечено (☑/■/●)
   - Запиши в таблицу только выбранное значение
   - Если отмечено несколько — запиши их через запятую
   - Если ни одно не отмечено — запиши то, что вписано от руки

5. ФОРМАТ ЗНАЧЕНИЙ:
   - Числа: как есть (с точкой или запятой)
   - Диапазоны: "1.6 ... 2.5"
   - Текст: как строку
   - Тире/прочерк: "-"
   - Пустые значения: пропускай строку

В ответе пришли ТОЛЬКО Markdown, без пояснений.
"""