from typing import Dict, Any

def get_promt(params: Dict[str, Any]) -> str:
    super_promt = f"""
    Мне нужно собрать json по шаблону:
    {params}

    из шаблона нужно взять названия параметров, найти совпадение в исходных данных из документа, который я прислал,
    нужно выбрать одно значение из доступных (там где предлагается список), это значение должно соответствовать исходному или самое близкое к нему (если среди предложенных его нет),
    если параметра из шаблона нет в исходных данных - пропусти и переходи к следующему.

    результирующий json должен состоять из пар ключ - значение, где ключи - и значения взяты из шаблона в соответсвии с исходным файлом.
    в ответ пришли только json!
    """
    return super_promt


VALIDATION_PROMPT = """
Ты инженер по подбору предохранительной арматуры. Сопоставь значения параметров из двух источников.

У тебя есть:
1. **RAW_MD** — Markdown-таблица с колонками | Параметр | Значение |. Параметры могут иметь вложенные подпараметры (строки, начинающиеся с "-").
2. **TEMPLATE_JSON** — эталонный словарь, где ключи — названия параметров, а значения — массив допустимых вариантов. Массив может содержать строки (например, "нержавеющая сталь (12Х18Н9ТЛ)") или числа.

Алгоритм:
1. **Парсинг RAW_MD**: преобразуй таблицу в структуру: для каждого параметра получи значение и отдельно размерность (если есть). 
    Размерность отделяется последней запятой: "100, мм" → значение "100", размерность "мм". Подпараметры (строки, начинающиеся с "-") привяжи к родительскому параметру.
2. **Поиск соответствия**: для каждого ключа из TEMPLATE_JSON найди наиболее подходящий параметр в RAW_MD по смыслу, синонимам, 
    частичному совпадению или точному совпадению названия. Если подходящий параметр найден, возьми его значение (без размерности). Если не найден — пропусти этот параметр (не включай в ответ).
    **Особое внимание параметру «Корпус»**: Корпус — это основная деталь проточной части клапана. 
    Крышка, золотник, пружина, уплотнение и другие элементы являются отдельными составными частями и имеют свои собственные параметры 
    (например, «Уплотнение», «Пружина», «Материал крышки» и т.д.). При поиске значения для «Корпус» выбирай только то значение, 
    которое явно относится к материалу корпуса. Если в RAW_MD указаны материалы для других деталей (крышка, золотник, пружина, уплотнение), 
    они не должны использоваться для параметра «Корпус». Если невозможно однозначно определить материал корпуса — пропусти параметр.
3. **Выбор допустимого значения**: из массива допустимых значений в TEMPLATE_JSON выбери значение:
   - Если значение из RAW_MD **совпадает** с любым элементом массива (или содержится как подстрока, или короткое название соответствует полному описанию, например "12Х18Н9ТЛ" → "нержавеющая сталь (12Х18Н9ТЛ)"), то используй именно тот элемент массива (он может быть полным описанием).
   - Если совпадений нет, но все элементы массива — числа, то выбери число, наиболее близкое к значению из RAW_MD (по абсолютной разнице).
   - Если совпадений нет и элементы массива не числа — пропусти параметр (не включай в ответ).
4. **Конвертация единиц измерения**: если значение из RAW_MD имеет размерность, а в TEMPLATE_JSON ожидаются другие единицы (определи по ключу параметра, например для давления — обычно кгс/см²), сконвертируй значение. Используй стандартные коэффициенты: 1 МПа = 10 кгс/см², 1 бар ≈ 1 кгс/см², 1 дюйм = 25.4 мм и т.д. Если размерность не указана или не требуется, оставь как есть.
5. **Формат ответа**: сформируй JSON-объект, где ключи — это ключи из TEMPLATE_JSON, а значения — выбранные и сконвертированные значения (без размерности). Включай только те параметры, для которых удалось подобрать значение. Порядок ключей — как в TEMPLATE_JSON.

В ответе пришли ТОЛЬКО JSON, без пояснений.
"""

VALIDATION_PROMPT = """
Ты инженер по подбору предохранительной арматуры. Сопоставь значения параметров из двух источников.

У тебя есть:
1. **RAW_MD** — Markdown-таблица с колонками | Параметр | Значение |. Параметры могут иметь вложенные подпараметры (строки, начинающиеся с "-").
2. **TEMPLATE_JSON** — эталонный словарь, где ключи — названия параметров, а значения — массив допустимых вариантов. Массив может содержать строки (например, "нержавеющая сталь (12Х18Н9ТЛ)") или числа.
3. **RULES_TABLE** — список объектов с полями "name" и "default". Эти параметры являются подмножеством TEMPLATE_JSON. Если какой-то параметр из RULES_TABLE не был найден в RAW_MD, он включается в ответ со значением default.

Алгоритм:
1. **Парсинг RAW_MD**: преобразуй таблицу в структуру: для каждого параметра получи значение и отдельно размерность (если есть). 
    Размерность отделяется последней запятой: "100, мм" → значение "100", размерность "мм". Подпараметры (строки, начинающиеся с "-") привяжи к родительскому параметру.

2. **Поиск соответствия**: для каждого ключа из TEMPLATE_JSON найди наиболее подходящий параметр в RAW_MD по смыслу, синонимам, 
    частичному совпадению или точному совпадению названия. Если подходящий параметр найден, возьми его значение (без размерности). Если не найден — пропусти этот параметр (не включай в ответ), если только он не присутствует в RULES_TABLE (тогда см. шаг 5).

    **Особое внимание параметру «Корпус»**: Корпус — это основная деталь проточной части клапана. 
    Крышка, золотник, пружина, уплотнение и другие элементы являются отдельными составными частями и имеют свои собственные параметры 
    (например, «Уплотнение», «Пружина», «Материал крышки» и т.д.). При поиске значения для «Корпус» выбирай только то значение, 
    которое явно относится к материалу корпуса. Если в RAW_MD указаны материалы для других деталей (крышка, золотник, пружина, уплотнение), 
    они не должны использоваться для параметра «Корпус». Если невозможно однозначно определить материал корпуса — пропусти параметр.

    **Особое правило для строительных параметров**: к ним относятся ключи TEMPLATE_JSON, содержащие слова "высота", "масса", "строительная длина L1", "строительная длина L2" (или их точные варианты). Для этих параметров:
    - Если в RAW_MD нет **точного совпадения по названию** (полное совпадение строки) — параметр игнорируется (не включается в ответ).
    - Если точное совпадение есть, но значение из RAW_MD **не совпадает ни с одним допустимым значением** из TEMPLATE_JSON (или TEMPLATE_JSON не содержит допустимых значений, например, там "*"), то параметр **включается** со значением из RAW_MD (после конвертации единиц, если требуется). При этом в итоговый JSON добавляется поле `"Примечание"` с текстом: "Для параметра <название_параметра> указано значение <значение>, которое не соответствует допустимым значениям."

3. **Выбор допустимого значения**: из массива допустимых значений в TEMPLATE_JSON выбери значение:
   - Если значение из RAW_MD **совпадает** с любым элементом массива (или содержится как подстрока, или короткое название соответствует полному описанию, например "12Х18Н9ТЛ" → "нержавеющая сталь (12Х18Н9ТЛ)"), то используй именно тот элемент массива (он может быть полным описанием).
   - Если совпадений нет, но все элементы массива — числа, то выбери число, наиболее близкое к значению из RAW_MD (по абсолютной разнице).
   - Если совпадений нет и элементы массива не числа — пропусти параметр (не включай в ответ), за исключением строительных параметров (см. шаг 2) и параметров из RULES_TABLE (см. шаг 5).

4. **Конвертация единиц измерения**: если значение из RAW_MD имеет размерность, а в TEMPLATE_JSON ожидаются другие единицы (определи по ключу параметра, например для давления — обычно кгс/см²), сконвертируй значение. Используй стандартные коэффициенты: 1 МПа = 10 кгс/см², 1 бар ≈ 1 кгс/см², 1 дюйм = 25.4 мм и т.д. Если размерность не указана или не требуется, оставь как есть.

5. **Обработка RULES_TABLE**: для каждого элемента из RULES_TABLE:
   - Если параметр с именем name **не был найден** в RAW_MD (т.е. не прошёл шаг 2 и не был включён в ответ), то **добавь** этот параметр в итоговый JSON со значением из поля default.
   - Если в RAW_MD удалось подобрать параметр "Материал корпуса", но не удалось найти параметр "Климатическое исполнение", то игнорируй параметр "Климатическое исполнение" из RULES_TABLE.
   - Если в RAW_MD удалось подобрать параметр "Климатическое исполнение", но не удалось найти параметр "Материал корпуса", то игнорируй параметр "Материал корпуса" из RULES_TABLE.

6. **Формат ответа**: сформируй JSON-объект, где ключи — это ключи из TEMPLATE_JSON (и, возможно, ключи из RULES_TABLE, которые также входят в TEMPLATE_JSON), а значения — выбранные и сконвертированные значения (без размерности). Включай только те параметры, для которых удалось подобрать значение, а также параметры из RULES_TABLE, добавленные по умолчанию. Если было добавлено поле "Примечание" (см. шаг 2), включи его как отдельный ключ верхнего уровня. Порядок ключей — как в TEMPLATE_JSON, затем "Примечание" в конце (если есть).

В ответе пришли ТОЛЬКО JSON, без пояснений.
"""

UNIFIED_PROMPT = """
Из документа, который я прислал, извлеки все параметры и их значения.
Верни результат строго в формате Markdown-таблицы с двумя колонками.

ПРАВИЛА:

1. Таблица: | Параметр | Значение |
2. В значении сначала значение, потом единица измерения через запятую, если есть.
3. Если параметр имеет несколько числовых значений с уточнениями (например, давление рабочее, настройки, расчётное) — оформи их как вложенный список с дефисом:
   | Давление (избыточное) | |
   | - Рабочее | 1.6, МПа |
   | - Настройка | 1.8, МПа |
4. Если в строке есть выбор из вариантов (например, «да/yes нет/no», «Колпак глухой / открытый», «с пружинной нагрузкой / с грузом») — выбери тот вариант, который явно отмечен (подчёркнут, жирный, обведён, отмечен галочкой). Если отметка не видна, но один вариант вписан от руки или повторяется в соседнем тексте — используй его. Если отмечено несколько — перечисли через запятую. Если ни один не отмечен — запиши все варианты через косую черту, как в документе.
5. Если значение не указано — пропускай строку или ставь прочерк «-».
6. Обязательно извлеки параметры из ВСЕХ разделов: Общие сведения, Рабочие условия, Расчётные условия, Требования к конструкции, Дополнительные требования и другие, если есть. Не пропускай ни один блок.
7. Текстовые перечисления (например, перечень документов) объединяй через запятую в одной строке.
8. Не добавляй пояснений, только таблица.
"""

RULES_TABLE = [
  { "name": "Устройство принудительного открытия", "default": "не требуется"},
  { "name": "Тип уплотнения", "default": "сальниковое"},
  { "name": "Тип конструкции ", "default": "Клапан пружинный" },
  { "name": "По способу сброса рабочей среды", "default": "открытый"},
  { "name": "Климатическое исполнение", "default": "У1 (40…+40 °C)"},
  { "name": "Испытания", "default": "-"},
  { "name": "Покраска", "default": "ТУ АО НПО Регулятор"},
  { "name": "Наличие КОФ", "default": "Без КОФ"},
  { "name": "Наличие ЗИП", "default": "Без ЗИП"},
  { "name": "Упаковка", "default": "на поддон" },
]